# v4.2 デモ検証補足ガイド

**🌐 Language:** **日本語** | [English](en/v4.2-demo-verification-supplement.md) | [한국어](ko/v4.2-demo-verification-supplement.md) | [简体中文](zh-CN/v4.2-demo-verification-supplement.md) | [繁體中文](zh-TW/v4.2-demo-verification-supplement.md) | [Français](fr/v4.2-demo-verification-supplement.md) | [Deutsch](de/v4.2-demo-verification-supplement.md) | [Español](es/v4.2-demo-verification-supplement.md)

**最終更新**: 2026-05-23  
**目的**: v4.2 各ユースケースの実環境テスト手順、期待結果、ログ取得方法

---

## 1. Smart Routing 検証

### テスト手順

1. CloudFront URL にアクセスし、admin ユーザーでサインイン
2. チャット設定で **Smart Routing** を有効化
3. 以下の3種類のクエリを送信し、ルーティング結果を確認

### テストクエリと期待結果

| クエリ | 期待ティア | 期待モデル | 確認ポイント |
|--------|-----------|-----------|-------------|
| 「こんにちは」 | Simple | Haiku 4.5 | レスポンスメタデータに `routingTier: simple` |
| 「製品カタログの内容を要約してください」 | Complex | Sonnet 3.5 v2 | レスポンスメタデータに `routingTier: complex` |
| 「全部門の四半期レポートを横断分析し、コスト削減の機会を特定してください」 | Full-context | Opus 4 | レスポンスメタデータに `routingTier: full-context` |

### CloudWatch メトリクス確認

```bash
# ルーティング分布の確認（過去1時間）
aws cloudwatch get-metric-statistics \
  --namespace SmartRouting \
  --metric-name RoutingCount \
  --dimensions Name=RoutingTier,Value=simple \
  --start-time $(date -u -v-1H +%Y-%m-%dT%H:%M:%S) \
  --end-time $(date -u +%Y-%m-%dT%H:%M:%S) \
  --period 300 \
  --statistics Sum \
  --region ap-northeast-1
```

### 期待される CloudWatch Logs 出力

```json
{
  "level": "INFO",
  "message": "Smart routing decision",
  "query": "こんにちは",
  "classification": "simple",
  "confidence": 0.95,
  "selectedModel": "anthropic.claude-haiku-4-5-20251001-v1:0",
  "queryWordCount": 1,
  "hasAnalyticalTerms": false
}
```

### スクリーンショット取得ポイント

- [ ] チャット UI でルーティングティアが表示されている画面
- [ ] CloudWatch メトリクスダッシュボードのルーティング分布グラフ
- [ ] 3ティアの応答時間比較（同一質問）

---

## 2. Transfer Family IAM Deny 検証

### テスト手順

パートナーユーザーが `.metadata.json` を上書きできないことを確認します。

```bash
# 1. SFTP接続
sftp -i /path/to/partner-key \
  -o HostKeyAlgorithms=rsa-sha2-256,rsa-sha2-512 \
  partner-a@s-XXXXXXXXXXXXXXXXX.server.transfer.ap-northeast-1.amazonaws.com

# 2. 通常ファイルのアップロード（成功するはず）
sftp> put test-document.pdf /uploads/partner-a/test-document.pdf

# 3. .metadata.json の上書き試行（拒否されるはず）
sftp> put fake-metadata.json /uploads/partner-a/test-document.pdf.metadata.json
```

### 期待結果

```
# 通常ファイル: 成功
Uploading test-document.pdf to /uploads/partner-a/test-document.pdf
test-document.pdf                                    100%  1234     12.3KB/s   00:00

# .metadata.json: Access Denied
Uploading fake-metadata.json to /uploads/partner-a/test-document.pdf.metadata.json
remote open("/uploads/partner-a/test-document.pdf.metadata.json"): Permission denied
```

### CloudTrail での確認

```bash
# Access Denied イベントの確認
aws cloudtrail lookup-events \
  --lookup-attributes AttributeKey=EventName,AttributeValue=PutObject \
  --start-time $(date -u -v-1H +%Y-%m-%dT%H:%M:%S) \
  --region ap-northeast-1 \
  --query 'Events[?contains(CloudTrailEvent, `AccessDenied`)].{Time:EventTime,User:Username}'
```

### スクリーンショット取得ポイント

- [ ] SFTP ターミナルで通常ファイルアップロード成功
- [ ] SFTP ターミナルで `.metadata.json` アップロード拒否
- [ ] CloudTrail の Access Denied イベント

---

## 3. KB Auto-Sync 検証

### テスト手順

1. S3 Access Point にテストファイルを追加
2. 5分以内にスキャンが実行されることを確認
3. インジェスションジョブが開始・完了することを確認

```bash
# 1. テストファイルをS3 APに追加
aws s3api put-object \
  --bucket arn:aws:s3:ap-northeast-1:ACCOUNT:accesspoint/AP_NAME \
  --key test-auto-sync/new-document.md \
  --body ./test-document.md \
  --region ap-northeast-1

# 2. 対応する .metadata.json を追加
aws s3api put-object \
  --bucket arn:aws:s3:ap-northeast-1:ACCOUNT:accesspoint/AP_NAME \
  --key test-auto-sync/new-document.md.metadata.json \
  --body ./test-metadata.json \
  --region ap-northeast-1

# 3. Lambda ログを監視（5分以内にスキャン実行）
aws logs tail /aws/lambda/${PREFIX}-kb-auto-sync \
  --follow --region ap-northeast-1
```

### 期待される Lambda ログ出力

```json
{
  "level": "INFO",
  "message": "Scan completed",
  "scanId": "scan-20260523-103000",
  "filesScanned": 15,
  "newFiles": 1,
  "changedFiles": 0,
  "unchangedFiles": 14,
  "triggerIngestion": true
}
```

```json
{
  "level": "INFO",
  "message": "Ingestion job started",
  "jobId": "XXXXXXXXXX",
  "knowledgeBaseId": "XXXXXXXXXX",
  "dataSourceId": "XXXXXXXXXX"
}
```

### DynamoDB インベントリ確認

```bash
# インベントリテーブルの最新レコード確認
aws dynamodb scan \
  --table-name ${PREFIX}-kb-auto-sync-inventory \
  --limit 5 \
  --region ap-northeast-1 \
  --query 'Items[*].{Key:fileKey.S,ETag:etag.S,LastModified:lastModified.S}'
```

### スクリーンショット取得ポイント

- [ ] CloudWatch Logs でスキャン結果ログ
- [ ] DynamoDB インベントリテーブルのレコード（スキャン前後）
- [ ] Bedrock KB コンソールでインジェスションジョブ完了

---

## 4. Capacity Guardrails 検証

### テスト手順

同じアクションを短時間に連続実行し、レート制限が発動することを確認します。

```bash
# Guardrails Lambda を直接呼び出し（テスト用）
for i in 1 2 3 4; do
  echo "=== Attempt $i ==="
  aws lambda invoke \
    --function-name ${PREFIX}-capacity-monitor \
    --payload '{"action": "volume_resize", "volume_id": "vol-test", "new_size_gb": 100}' \
    --region ap-northeast-1 \
    /tmp/guardrail-response-$i.json
  cat /tmp/guardrail-response-$i.json
  echo ""
  sleep 2
done
```

### 期待結果

```
=== Attempt 1 ===
{"decision": "ALLOWED", "reason": "All checks passed", "action_count": 1}

=== Attempt 2 ===
{"decision": "ALLOWED", "reason": "All checks passed", "action_count": 2}

=== Attempt 3 ===
{"decision": "ALLOWED", "reason": "All checks passed", "action_count": 3}

=== Attempt 4 ===
{"decision": "BLOCKED", "reason": "Rate limit exceeded: max 3 per hour for volume_resize", "action_count": 3}
```

### CloudWatch メトリクス確認

```bash
# ガードレール判定メトリクス
aws cloudwatch get-metric-statistics \
  --namespace FSxNOps/Guardrails \
  --metric-name GuardrailDecision \
  --dimensions Name=Action,Value=volume_resize Name=Decision,Value=BLOCKED \
  --start-time $(date -u -v-1H +%Y-%m-%dT%H:%M:%S) \
  --end-time $(date -u +%Y-%m-%dT%H:%M:%S) \
  --period 300 \
  --statistics Sum \
  --region ap-northeast-1
```

### DynamoDB 状態確認

```bash
# ガードレール状態テーブルのレコード確認
aws dynamodb get-item \
  --table-name ${PREFIX}-guardrail-state \
  --key '{"action_name": {"S": "volume_resize"}}' \
  --region ap-northeast-1
```

### 期待される DynamoDB レコード

```json
{
  "action_name": {"S": "volume_resize"},
  "action_count": {"N": "3"},
  "window_start": {"S": "2026-05-23T10:00:00Z"},
  "last_execution": {"S": "2026-05-23T10:05:00Z"},
  "ttl": {"N": "1716544800"}
}
```

### スクリーンショット取得ポイント

- [ ] Lambda 実行ログ（ALLOWED → BLOCKED の遷移）
- [ ] DynamoDB テーブルのレコード（TTL付き）
- [ ] CloudWatch メトリクスの BLOCKED カウント

---

## 5. Voice Chat WebRTC 検証

### 前提条件

- `enableVoiceChat=true` でデプロイ済み
- Pipecat Voice Agent が AgentCore Runtime にデプロイ済み（CLI）
- KVS Signaling Channel が作成済み

### テスト手順

1. CloudFront URL にアクセスし、サインイン
2. チャット画面でマイクアイコンをクリック
3. 音声入力 → テキスト変換 → RAG検索 → 音声出力のフローを確認

### 確認ポイント

| ステップ | 確認内容 |
|---------|---------|
| WebRTC接続 | KVS Signaling Channel 経由で接続確立 |
| 音声入力 | マイクからの音声がキャプチャされる |
| STT | 音声がテキストに変換される |
| RAG検索 | 変換されたテキストで KB 検索が実行される |
| TTS | 検索結果が音声で返される |
| フォールバック | WebRTC失敗時にREST APIに自動切替 |

### スクリーンショット取得ポイント

- [ ] Voice Chat UI（マイク入力中）
- [ ] KVS Signaling Channel コンソール画面
- [ ] WebRTC → REST フォールバック時のログ

---

## 6. 検証結果テンプレート

各検証完了後、以下のテンプレートで結果を記録してください:

```markdown
## 検証結果: [ユースケース名]

**検証日**: YYYY-MM-DD
**検証者**: [名前]
**環境**: [スタックプレフィックス] / [リージョン]

### 結果サマリ

| テスト項目 | 結果 | 備考 |
|-----------|------|------|
| [項目1] | ✅/❌ | [詳細] |

### 取得ログ（マスク済み）

[CloudWatch Logs / DynamoDB レコード / ターミナル出力]

### スクリーンショット

[マスク済みスクリーンショットへのリンク]

### 発見事項・改善点

- [発見事項1]
- [発見事項2]
```

---

## 7. マスキングルール

公開前に以下の情報をマスクしてください:

| 対象 | マスク方法 |
|------|-----------|
| AWS アカウントID | `XXXXXXXXXXXX` に置換 |
| リソースARN のアカウント部分 | `arn:aws:...:XXXXXXXXXXXX:...` |
| IPアドレス | `xxx.xxx.xxx.xxx` に置換 |
| SSH鍵のフィンガープリント | 削除 |
| Cognito User Pool ID | `ap-northeast-1_XXXXXXXXX` に置換 |
| Transfer Family Server ID | `s-XXXXXXXXXXXXXXXXX` に置換 |
| KB ID / Data Source ID | `XXXXXXXXXX` に置換 |

スクリーンショットのマスキングには既存のスクリプトを使用:

```bash
python3 docs/screenshots/mask_screenshots.py --input screenshot.png --output masked-screenshot.png
```
