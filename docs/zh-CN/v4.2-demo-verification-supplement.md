# v4.2 演示验证补充指南

**🌐 Language:** [日本語](../v4.2-demo-verification-supplement.md) | [English](../en/v4.2-demo-verification-supplement.md) | [한국어](../ko/v4.2-demo-verification-supplement.md) | **简体中文** | [繁體中文](../zh-TW/v4.2-demo-verification-supplement.md) | [Français](../fr/v4.2-demo-verification-supplement.md) | [Deutsch](../de/v4.2-demo-verification-supplement.md) | [Español](../es/v4.2-demo-verification-supplement.md)

**最后更新**: 2026-05-23  
**目的**: v4.2 各用例的真实环境测试步骤、预期结果、日志收集方法

---

## 1. Smart Routing 验证

### 测试步骤

1. 访问 CloudFront URL 并以 admin 用户登录
2. 在聊天设置中启用 **Smart Routing**
3. 发送以下 3 种类型的查询并确认路由结果

### 测试查询与预期结果

| 查询 | 预期层级 | 预期模型 | 确认要点 |
|--------|-----------|-----------|-------------|
| "你好" | Simple | Haiku 4.5 | 响应元数据中包含 `routingTier: simple` |
| "请总结产品目录的内容" | Complex | Sonnet 3.5 v2 | 响应元数据中包含 `routingTier: complex` |
| "请横向分析所有部门的季度报告并识别降本机会" | Full-context | Opus 4 | 响应元数据中包含 `routingTier: full-context` |

### CloudWatch 指标确认

```bash
# 确认路由分布（过去 1 小时）
aws cloudwatch get-metric-statistics \
  --namespace SmartRouting \
  --metric-name RoutingCount \
  --dimensions Name=RoutingTier,Value=simple \
  --start-time $(date -u -v-1H +%Y-%m-%dT%H:%M:%S) \
  --end-time $(date -u +%Y-%m-%dT%H:%M:%S) \
  --period 300 \
  --statistics Sum \
  --region ap-northeast-1
```

### 预期的 CloudWatch Logs 输出

```json
{
  "level": "INFO",
  "message": "Smart routing decision",
  "query": "你好",
  "classification": "simple",
  "confidence": 0.95,
  "selectedModel": "anthropic.claude-haiku-4-5-20251001-v1:0",
  "queryWordCount": 1,
  "hasAnalyticalTerms": false
}
```

### 截图采集要点

- [ ] 聊天 UI 中显示路由层级的画面
- [ ] CloudWatch 指标仪表板的路由分布图
- [ ] 3 个层级的响应时间对比（相同问题）

---

## 2. Transfer Family IAM Deny 验证

### 测试步骤

确认合作伙伴用户无法覆盖 `.metadata.json`。

```bash
# 1. SFTP 连接
sftp -i /path/to/partner-key \
  -o HostKeyAlgorithms=rsa-sha2-256,rsa-sha2-512 \
  partner-a@s-XXXXXXXXXXXXXXXXX.server.transfer.ap-northeast-1.amazonaws.com

# 2. 普通文件上传（应成功）
sftp> put test-document.pdf /uploads/partner-a/test-document.pdf

# 3. 尝试覆盖 .metadata.json（应被拒绝）
sftp> put fake-metadata.json /uploads/partner-a/test-document.pdf.metadata.json
```

### 预期结果

```
# 普通文件: 成功
Uploading test-document.pdf to /uploads/partner-a/test-document.pdf
test-document.pdf                                    100%  1234     12.3KB/s   00:00

# .metadata.json: Access Denied
Uploading fake-metadata.json to /uploads/partner-a/test-document.pdf.metadata.json
remote open("/uploads/partner-a/test-document.pdf.metadata.json"): Permission denied
```

### 在 CloudTrail 中确认

```bash
# 确认 Access Denied 事件
aws cloudtrail lookup-events \
  --lookup-attributes AttributeKey=EventName,AttributeValue=PutObject \
  --start-time $(date -u -v-1H +%Y-%m-%dT%H:%M:%S) \
  --region ap-northeast-1 \
  --query 'Events[?contains(CloudTrailEvent, `AccessDenied`)].{Time:EventTime,User:Username}'
```

### 截图采集要点

- [ ] SFTP 终端中普通文件上传成功
- [ ] SFTP 终端中 `.metadata.json` 上传被拒绝
- [ ] CloudTrail 的 Access Denied 事件

---

## 3. KB Auto-Sync 验证

### 测试步骤

1. 向 S3 Access Point 添加测试文件
2. 确认 5 分钟内执行扫描
3. 确认 ingestion 任务启动并完成

```bash
# 1. 向 S3 AP 添加测试文件
aws s3api put-object \
  --bucket arn:aws:s3:ap-northeast-1:ACCOUNT:accesspoint/AP_NAME \
  --key test-auto-sync/new-document.md \
  --body ./test-document.md \
  --region ap-northeast-1

# 2. 添加对应的 .metadata.json
aws s3api put-object \
  --bucket arn:aws:s3:ap-northeast-1:ACCOUNT:accesspoint/AP_NAME \
  --key test-auto-sync/new-document.md.metadata.json \
  --body ./test-metadata.json \
  --region ap-northeast-1

# 3. 监控 Lambda 日志（5 分钟内执行扫描）
aws logs tail /aws/lambda/${PREFIX}-kb-auto-sync \
  --follow --region ap-northeast-1
```

### 预期的 Lambda 日志输出

```json
{
  "level": "INFO",
  "message": "Scan completed",
  "scanId": "scan-20260523-103000",
  "filesScanned": 15,
  "newFiles": 1,
  "changedFiles": 0,
  "unchangedFiles": 14,
  "triggerIngestion": true
}
```

```json
{
  "level": "INFO",
  "message": "Ingestion job started",
  "jobId": "XXXXXXXXXX",
  "knowledgeBaseId": "XXXXXXXXXX",
  "dataSourceId": "XXXXXXXXXX"
}
```

### DynamoDB 库存确认

```bash
# 确认库存表的最新记录
aws dynamodb scan \
  --table-name ${PREFIX}-kb-auto-sync-inventory \
  --limit 5 \
  --region ap-northeast-1 \
  --query 'Items[*].{Key:fileKey.S,ETag:etag.S,LastModified:lastModified.S}'
```

### 截图采集要点

- [ ] CloudWatch Logs 中的扫描结果日志
- [ ] DynamoDB 库存表记录（扫描前后）
- [ ] Bedrock KB 控制台中 ingestion 任务完成

---

## 4. Capacity Guardrails 验证

### 测试步骤

在短时间内连续执行相同的操作，确认触发速率限制。

```bash
# 直接调用 Guardrails Lambda（用于测试）
for i in 1 2 3 4; do
  echo "=== Attempt $i ==="
  aws lambda invoke \
    --function-name ${PREFIX}-capacity-monitor \
    --payload '{"action": "volume_resize", "volume_id": "vol-test", "new_size_gb": 100}' \
    --region ap-northeast-1 \
    /tmp/guardrail-response-$i.json
  cat /tmp/guardrail-response-$i.json
  echo ""
  sleep 2
done
```

### 预期结果

```
=== Attempt 1 ===
{"decision": "ALLOWED", "reason": "All checks passed", "action_count": 1}

=== Attempt 2 ===
{"decision": "ALLOWED", "reason": "All checks passed", "action_count": 2}

=== Attempt 3 ===
{"decision": "ALLOWED", "reason": "All checks passed", "action_count": 3}

=== Attempt 4 ===
{"decision": "BLOCKED", "reason": "Rate limit exceeded: max 3 per hour for volume_resize", "action_count": 3}
```

### CloudWatch 指标确认

```bash
# 护栏判定指标
aws cloudwatch get-metric-statistics \
  --namespace FSxNOps/Guardrails \
  --metric-name GuardrailDecision \
  --dimensions Name=Action,Value=volume_resize Name=Decision,Value=BLOCKED \
  --start-time $(date -u -v-1H +%Y-%m-%dT%H:%M:%S) \
  --end-time $(date -u +%Y-%m-%dT%H:%M:%S) \
  --period 300 \
  --statistics Sum \
  --region ap-northeast-1
```

### DynamoDB 状态确认

```bash
# 确认护栏状态表的记录
aws dynamodb get-item \
  --table-name ${PREFIX}-guardrail-state \
  --key '{"action_name": {"S": "volume_resize"}}' \
  --region ap-northeast-1
```

### 预期的 DynamoDB 记录

```json
{
  "action_name": {"S": "volume_resize"},
  "action_count": {"N": "3"},
  "window_start": {"S": "2026-05-23T10:00:00Z"},
  "last_execution": {"S": "2026-05-23T10:05:00Z"},
  "ttl": {"N": "1716544800"}
}
```

### 截图采集要点

- [ ] Lambda 执行日志（ALLOWED → BLOCKED 的转变）
- [ ] DynamoDB 表记录（带 TTL）
- [ ] CloudWatch 指标的 BLOCKED 计数

---

## 5. Voice Chat WebRTC 验证

### 前提条件

- 已使用 `enableVoiceChat=true` 部署
- Pipecat Voice Agent 已部署到 AgentCore Runtime（CLI）
- 已创建 KVS Signaling Channel

### 测试步骤

1. 访问 CloudFront URL 并登录
2. 在聊天画面点击麦克风图标
3. 确认语音输入 → 文本转换 → RAG 检索 → 语音输出的流程

### 确认要点

| 步骤 | 确认内容 |
|---------|---------|
| WebRTC 连接 | 通过 KVS Signaling Channel 建立连接 |
| 语音输入 | 从麦克风捕获语音 |
| STT | 语音转换为文本 |
| RAG 检索 | 使用转换后的文本执行 KB 检索 |
| TTS | 检索结果以语音返回 |
| 回退 | WebRTC 失败时自动切换到 REST API |

### 截图采集要点

- [ ] Voice Chat UI（麦克风输入中）
- [ ] KVS Signaling Channel 控制台画面
- [ ] WebRTC → REST 回退时的日志

---

## 6. 验证结果模板

每次验证完成后，请使用以下模板记录结果:

```markdown
## 验证结果: [用例名称]

**验证日期**: YYYY-MM-DD
**验证人**: [姓名]
**环境**: [堆栈前缀] / [区域]

### 结果摘要

| 测试项 | 结果 | 备注 |
|-----------|------|------|
| [项目 1] | ✅/❌ | [详情] |

### 收集的日志（已脱敏）

[CloudWatch Logs / DynamoDB 记录 / 终端输出]

### 截图

[已脱敏截图的链接]

### 发现事项与改进点

- [发现事项 1]
- [发现事项 2]
```

---

## 7. 脱敏规则

发布前请脱敏以下信息:

| 对象 | 脱敏方法 |
|------|-----------|
| AWS 账户 ID | 替换为 `XXXXXXXXXXXX` |
| 资源 ARN 的账户部分 | `arn:aws:...:XXXXXXXXXXXX:...` |
| IP 地址 | 替换为 `xxx.xxx.xxx.xxx` |
| SSH 密钥指纹 | 删除 |
| Cognito User Pool ID | 替换为 `ap-northeast-1_XXXXXXXXX` |
| Transfer Family Server ID | 替换为 `s-XXXXXXXXXXXXXXXXX` |
| KB ID / Data Source ID | 替换为 `XXXXXXXXXX` |

截图脱敏请使用现有脚本:

```bash
python3 docs/screenshots/mask_screenshots.py --input screenshot.png --output masked-screenshot.png
```
