# v4.2 示範驗證補充指南

**🌐 Language:** [日本語](../v4.2-demo-verification-supplement.md) | [English](../en/v4.2-demo-verification-supplement.md) | [한국어](../ko/v4.2-demo-verification-supplement.md) | [简体中文](../zh-CN/v4.2-demo-verification-supplement.md) | **繁體中文** | [Français](../fr/v4.2-demo-verification-supplement.md) | [Deutsch](../de/v4.2-demo-verification-supplement.md) | [Español](../es/v4.2-demo-verification-supplement.md)

**最後更新**: 2026-05-23  
**目的**: v4.2 各使用案例的真實環境測試步驟、預期結果、日誌收集方法

---

## 1. Smart Routing 驗證

### 測試步驟

1. 存取 CloudFront URL 並以 admin 使用者登入
2. 在聊天設定中啟用 **Smart Routing**
3. 傳送以下 3 種類型的查詢並確認路由結果

### 測試查詢與預期結果

| 查詢 | 預期層級 | 預期模型 | 確認要點 |
|--------|-----------|-----------|-------------|
| "你好" | Simple | Haiku 4.5 | 回應中繼資料中包含 `routingTier: simple` |
| "請總結產品型錄的內容" | Complex | Sonnet 3.5 v2 | 回應中繼資料中包含 `routingTier: complex` |
| "請橫向分析所有部門的季度報告並找出降低成本的機會" | Full-context | Opus 4 | 回應中繼資料中包含 `routingTier: full-context` |

### CloudWatch 指標確認

```bash
# 確認路由分佈（過去 1 小時）
aws cloudwatch get-metric-statistics \
  --namespace SmartRouting \
  --metric-name RoutingCount \
  --dimensions Name=RoutingTier,Value=simple \
  --start-time $(date -u -v-1H +%Y-%m-%dT%H:%M:%S) \
  --end-time $(date -u +%Y-%m-%dT%H:%M:%S) \
  --period 300 \
  --statistics Sum \
  --region ap-northeast-1
```

### 預期的 CloudWatch Logs 輸出

```json
{
  "level": "INFO",
  "message": "Smart routing decision",
  "query": "你好",
  "classification": "simple",
  "confidence": 0.95,
  "selectedModel": "anthropic.claude-haiku-4-5-20251001-v1:0",
  "queryWordCount": 1,
  "hasAnalyticalTerms": false
}
```

### 螢幕截圖擷取要點

- [ ] 聊天 UI 中顯示路由層級的畫面
- [ ] CloudWatch 指標儀表板的路由分佈圖
- [ ] 3 個層級的回應時間比較（相同問題）

---

## 2. Transfer Family IAM Deny 驗證

### 測試步驟

確認合作夥伴使用者無法覆寫 `.metadata.json`。

```bash
# 1. SFTP 連線
sftp -i /path/to/partner-key \
  -o HostKeyAlgorithms=rsa-sha2-256,rsa-sha2-512 \
  partner-a@s-XXXXXXXXXXXXXXXXX.server.transfer.ap-northeast-1.amazonaws.com

# 2. 一般檔案上傳（應成功）
sftp> put test-document.pdf /uploads/partner-a/test-document.pdf

# 3. 嘗試覆寫 .metadata.json（應被拒絕）
sftp> put fake-metadata.json /uploads/partner-a/test-document.pdf.metadata.json
```

### 預期結果

```
# 一般檔案: 成功
Uploading test-document.pdf to /uploads/partner-a/test-document.pdf
test-document.pdf                                    100%  1234     12.3KB/s   00:00

# .metadata.json: Access Denied
Uploading fake-metadata.json to /uploads/partner-a/test-document.pdf.metadata.json
remote open("/uploads/partner-a/test-document.pdf.metadata.json"): Permission denied
```

### 在 CloudTrail 中確認

```bash
# 確認 Access Denied 事件
aws cloudtrail lookup-events \
  --lookup-attributes AttributeKey=EventName,AttributeValue=PutObject \
  --start-time $(date -u -v-1H +%Y-%m-%dT%H:%M:%S) \
  --region ap-northeast-1 \
  --query 'Events[?contains(CloudTrailEvent, `AccessDenied`)].{Time:EventTime,User:Username}'
```

### 螢幕截圖擷取要點

- [ ] SFTP 終端機中一般檔案上傳成功
- [ ] SFTP 終端機中 `.metadata.json` 上傳被拒絕
- [ ] CloudTrail 的 Access Denied 事件

---

## 3. KB Auto-Sync 驗證

### 測試步驟

1. 向 S3 Access Point 新增測試檔案
2. 確認 5 分鐘內執行掃描
3. 確認 ingestion 作業啟動並完成

```bash
# 1. 向 S3 AP 新增測試檔案
aws s3api put-object \
  --bucket arn:aws:s3:ap-northeast-1:ACCOUNT:accesspoint/AP_NAME \
  --key test-auto-sync/new-document.md \
  --body ./test-document.md \
  --region ap-northeast-1

# 2. 新增對應的 .metadata.json
aws s3api put-object \
  --bucket arn:aws:s3:ap-northeast-1:ACCOUNT:accesspoint/AP_NAME \
  --key test-auto-sync/new-document.md.metadata.json \
  --body ./test-metadata.json \
  --region ap-northeast-1

# 3. 監控 Lambda 日誌（5 分鐘內執行掃描）
aws logs tail /aws/lambda/${PREFIX}-kb-auto-sync \
  --follow --region ap-northeast-1
```

### 預期的 Lambda 日誌輸出

```json
{
  "level": "INFO",
  "message": "Scan completed",
  "scanId": "scan-20260523-103000",
  "filesScanned": 15,
  "newFiles": 1,
  "changedFiles": 0,
  "unchangedFiles": 14,
  "triggerIngestion": true
}
```

```json
{
  "level": "INFO",
  "message": "Ingestion job started",
  "jobId": "XXXXXXXXXX",
  "knowledgeBaseId": "XXXXXXXXXX",
  "dataSourceId": "XXXXXXXXXX"
}
```

### DynamoDB 庫存確認

```bash
# 確認庫存表的最新記錄
aws dynamodb scan \
  --table-name ${PREFIX}-kb-auto-sync-inventory \
  --limit 5 \
  --region ap-northeast-1 \
  --query 'Items[*].{Key:fileKey.S,ETag:etag.S,LastModified:lastModified.S}'
```

### 螢幕截圖擷取要點

- [ ] CloudWatch Logs 中的掃描結果日誌
- [ ] DynamoDB 庫存表記錄（掃描前後）
- [ ] Bedrock KB 主控台中 ingestion 作業完成

---

## 4. Capacity Guardrails 驗證

### 測試步驟

在短時間內連續執行相同的動作，確認觸發速率限制。

```bash
# 直接呼叫 Guardrails Lambda（用於測試）
for i in 1 2 3 4; do
  echo "=== Attempt $i ==="
  aws lambda invoke \
    --function-name ${PREFIX}-capacity-monitor \
    --payload '{"action": "volume_resize", "volume_id": "vol-test", "new_size_gb": 100}' \
    --region ap-northeast-1 \
    /tmp/guardrail-response-$i.json
  cat /tmp/guardrail-response-$i.json
  echo ""
  sleep 2
done
```

### 預期結果

```
=== Attempt 1 ===
{"decision": "ALLOWED", "reason": "All checks passed", "action_count": 1}

=== Attempt 2 ===
{"decision": "ALLOWED", "reason": "All checks passed", "action_count": 2}

=== Attempt 3 ===
{"decision": "ALLOWED", "reason": "All checks passed", "action_count": 3}

=== Attempt 4 ===
{"decision": "BLOCKED", "reason": "Rate limit exceeded: max 3 per hour for volume_resize", "action_count": 3}
```

### CloudWatch 指標確認

```bash
# 護欄判定指標
aws cloudwatch get-metric-statistics \
  --namespace FSxNOps/Guardrails \
  --metric-name GuardrailDecision \
  --dimensions Name=Action,Value=volume_resize Name=Decision,Value=BLOCKED \
  --start-time $(date -u -v-1H +%Y-%m-%dT%H:%M:%S) \
  --end-time $(date -u +%Y-%m-%dT%H:%M:%S) \
  --period 300 \
  --statistics Sum \
  --region ap-northeast-1
```

### DynamoDB 狀態確認

```bash
# 確認護欄狀態表的記錄
aws dynamodb get-item \
  --table-name ${PREFIX}-guardrail-state \
  --key '{"action_name": {"S": "volume_resize"}}' \
  --region ap-northeast-1
```

### 預期的 DynamoDB 記錄

```json
{
  "action_name": {"S": "volume_resize"},
  "action_count": {"N": "3"},
  "window_start": {"S": "2026-05-23T10:00:00Z"},
  "last_execution": {"S": "2026-05-23T10:05:00Z"},
  "ttl": {"N": "1716544800"}
}
```

### 螢幕截圖擷取要點

- [ ] Lambda 執行日誌（ALLOWED → BLOCKED 的轉變）
- [ ] DynamoDB 表記錄（帶 TTL）
- [ ] CloudWatch 指標的 BLOCKED 計數

---

## 5. Voice Chat WebRTC 驗證

### 前提條件

- 已使用 `enableVoiceChat=true` 部署
- Pipecat Voice Agent 已部署到 AgentCore Runtime（CLI）
- 已建立 KVS Signaling Channel

### 測試步驟

1. 存取 CloudFront URL 並登入
2. 在聊天畫面點擊麥克風圖示
3. 確認語音輸入 → 文字轉換 → RAG 檢索 → 語音輸出的流程

### 確認要點

| 步驟 | 確認內容 |
|---------|---------|
| WebRTC 連線 | 透過 KVS Signaling Channel 建立連線 |
| 語音輸入 | 從麥克風擷取語音 |
| STT | 語音轉換為文字 |
| RAG 檢索 | 使用轉換後的文字執行 KB 檢索 |
| TTS | 檢索結果以語音返回 |
| 回退 | WebRTC 失敗時自動切換到 REST API |

### 螢幕截圖擷取要點

- [ ] Voice Chat UI（麥克風輸入中）
- [ ] KVS Signaling Channel 主控台畫面
- [ ] WebRTC → REST 回退時的日誌

---

## 6. 驗證結果範本

每次驗證完成後，請使用以下範本記錄結果:

```markdown
## 驗證結果: [使用案例名稱]

**驗證日期**: YYYY-MM-DD
**驗證人**: [姓名]
**環境**: [堆疊前綴] / [區域]

### 結果摘要

| 測試項 | 結果 | 備註 |
|-----------|------|------|
| [項目 1] | ✅/❌ | [詳情] |

### 收集的日誌（已遮罩）

[CloudWatch Logs / DynamoDB 記錄 / 終端機輸出]

### 螢幕截圖

[已遮罩螢幕截圖的連結]

### 發現事項與改善點

- [發現事項 1]
- [發現事項 2]
```

---

## 7. 遮罩規則

發布前請遮罩以下資訊:

| 對象 | 遮罩方法 |
|------|-----------|
| AWS 帳戶 ID | 取代為 `XXXXXXXXXXXX` |
| 資源 ARN 的帳戶部分 | `arn:aws:...:XXXXXXXXXXXX:...` |
| IP 位址 | 取代為 `xxx.xxx.xxx.xxx` |
| SSH 金鑰指紋 | 刪除 |
| Cognito User Pool ID | 取代為 `ap-northeast-1_XXXXXXXXX` |
| Transfer Family Server ID | 取代為 `s-XXXXXXXXXXXXXXXXX` |
| KB ID / Data Source ID | 取代為 `XXXXXXXXXX` |

螢幕截圖遮罩請使用現有指令碼:

```bash
python3 docs/screenshots/mask_screenshots.py --input screenshot.png --output masked-screenshot.png
```
