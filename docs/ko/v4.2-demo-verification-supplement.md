# v4.2 데모 검증 보충 가이드

**🌐 Language:** [日本語](../v4.2-demo-verification-supplement.md) | [English](../en/v4.2-demo-verification-supplement.md) | **한국어** | [简体中文](../zh-CN/v4.2-demo-verification-supplement.md) | [繁體中文](../zh-TW/v4.2-demo-verification-supplement.md) | [Français](../fr/v4.2-demo-verification-supplement.md) | [Deutsch](../de/v4.2-demo-verification-supplement.md) | [Español](../es/v4.2-demo-verification-supplement.md)

**최종 업데이트**: 2026-05-23  
**목적**: v4.2 각 유스케이스의 실제 환경 테스트 절차, 예상 결과, 로그 수집 방법

---

## 1. Smart Routing 검증

### 테스트 절차

1. CloudFront URL에 접속하여 admin 사용자로 로그인
2. 채팅 설정에서 **Smart Routing** 활성화
3. 다음 3가지 유형의 쿼리를 전송하고 라우팅 결과 확인

### 테스트 쿼리 및 예상 결과

| 쿼리 | 예상 티어 | 예상 모델 | 확인 포인트 |
|--------|-----------|-----------|-------------|
| "안녕하세요" | Simple | Haiku 4.5 | 응답 메타데이터에 `routingTier: simple` |
| "제품 카탈로그 내용을 요약해 주세요" | Complex | Sonnet 3.5 v2 | 응답 메타데이터에 `routingTier: complex` |
| "전체 부서의 분기별 보고서를 횡단 분석하여 비용 절감 기회를 식별해 주세요" | Full-context | Opus 4 | 응답 메타데이터에 `routingTier: full-context` |

### CloudWatch 메트릭 확인

```bash
# 라우팅 분포 확인 (지난 1시간)
aws cloudwatch get-metric-statistics \
  --namespace SmartRouting \
  --metric-name RoutingCount \
  --dimensions Name=RoutingTier,Value=simple \
  --start-time $(date -u -v-1H +%Y-%m-%dT%H:%M:%S) \
  --end-time $(date -u +%Y-%m-%dT%H:%M:%S) \
  --period 300 \
  --statistics Sum \
  --region ap-northeast-1
```

### 예상 CloudWatch Logs 출력

```json
{
  "level": "INFO",
  "message": "Smart routing decision",
  "query": "안녕하세요",
  "classification": "simple",
  "confidence": 0.95,
  "selectedModel": "anthropic.claude-haiku-4-5-20251001-v1:0",
  "queryWordCount": 1,
  "hasAnalyticalTerms": false
}
```

### 스크린샷 캡처 포인트

- [ ] 채팅 UI에서 라우팅 티어가 표시되는 화면
- [ ] CloudWatch 메트릭 대시보드의 라우팅 분포 그래프
- [ ] 3개 티어의 응답 시간 비교 (동일 질문)

---

## 2. Transfer Family IAM Deny 검증

### 테스트 절차

파트너 사용자가 `.metadata.json`을 덮어쓸 수 없음을 확인합니다.

```bash
# 1. SFTP 연결
sftp -i /path/to/partner-key \
  -o HostKeyAlgorithms=rsa-sha2-256,rsa-sha2-512 \
  partner-a@s-XXXXXXXXXXXXXXXXX.server.transfer.ap-northeast-1.amazonaws.com

# 2. 일반 파일 업로드 (성공해야 함)
sftp> put test-document.pdf /uploads/partner-a/test-document.pdf

# 3. .metadata.json 덮어쓰기 시도 (거부되어야 함)
sftp> put fake-metadata.json /uploads/partner-a/test-document.pdf.metadata.json
```

### 예상 결과

```
# 일반 파일: 성공
Uploading test-document.pdf to /uploads/partner-a/test-document.pdf
test-document.pdf                                    100%  1234     12.3KB/s   00:00

# .metadata.json: Access Denied
Uploading fake-metadata.json to /uploads/partner-a/test-document.pdf.metadata.json
remote open("/uploads/partner-a/test-document.pdf.metadata.json"): Permission denied
```

### CloudTrail에서 확인

```bash
# Access Denied 이벤트 확인
aws cloudtrail lookup-events \
  --lookup-attributes AttributeKey=EventName,AttributeValue=PutObject \
  --start-time $(date -u -v-1H +%Y-%m-%dT%H:%M:%S) \
  --region ap-northeast-1 \
  --query 'Events[?contains(CloudTrailEvent, `AccessDenied`)].{Time:EventTime,User:Username}'
```

### 스크린샷 캡처 포인트

- [ ] SFTP 터미널에서 일반 파일 업로드 성공
- [ ] SFTP 터미널에서 `.metadata.json` 업로드 거부
- [ ] CloudTrail의 Access Denied 이벤트

---

## 3. KB Auto-Sync 검증

### 테스트 절차

1. S3 Access Point에 테스트 파일 추가
2. 5분 이내에 스캔이 실행되는지 확인
3. 인제스션 작업이 시작·완료되는지 확인

```bash
# 1. 테스트 파일을 S3 AP에 추가
aws s3api put-object \
  --bucket arn:aws:s3:ap-northeast-1:ACCOUNT:accesspoint/AP_NAME \
  --key test-auto-sync/new-document.md \
  --body ./test-document.md \
  --region ap-northeast-1

# 2. 대응하는 .metadata.json 추가
aws s3api put-object \
  --bucket arn:aws:s3:ap-northeast-1:ACCOUNT:accesspoint/AP_NAME \
  --key test-auto-sync/new-document.md.metadata.json \
  --body ./test-metadata.json \
  --region ap-northeast-1

# 3. Lambda 로그 모니터링 (5분 이내 스캔 실행)
aws logs tail /aws/lambda/${PREFIX}-kb-auto-sync \
  --follow --region ap-northeast-1
```

### 예상 Lambda 로그 출력

```json
{
  "level": "INFO",
  "message": "Scan completed",
  "scanId": "scan-20260523-103000",
  "filesScanned": 15,
  "newFiles": 1,
  "changedFiles": 0,
  "unchangedFiles": 14,
  "triggerIngestion": true
}
```

```json
{
  "level": "INFO",
  "message": "Ingestion job started",
  "jobId": "XXXXXXXXXX",
  "knowledgeBaseId": "XXXXXXXXXX",
  "dataSourceId": "XXXXXXXXXX"
}
```

### DynamoDB 인벤토리 확인

```bash
# 인벤토리 테이블의 최신 레코드 확인
aws dynamodb scan \
  --table-name ${PREFIX}-kb-auto-sync-inventory \
  --limit 5 \
  --region ap-northeast-1 \
  --query 'Items[*].{Key:fileKey.S,ETag:etag.S,LastModified:lastModified.S}'
```

### 스크린샷 캡처 포인트

- [ ] CloudWatch Logs에서 스캔 결과 로그
- [ ] DynamoDB 인벤토리 테이블 레코드 (스캔 전후)
- [ ] Bedrock KB 콘솔에서 인제스션 작업 완료

---

## 4. Capacity Guardrails 검증

### 테스트 절차

동일한 액션을 짧은 시간에 연속 실행하여 레이트 제한이 발동되는지 확인합니다.

```bash
# Guardrails Lambda를 직접 호출 (테스트용)
for i in 1 2 3 4; do
  echo "=== Attempt $i ==="
  aws lambda invoke \
    --function-name ${PREFIX}-capacity-monitor \
    --payload '{"action": "volume_resize", "volume_id": "vol-test", "new_size_gb": 100}' \
    --region ap-northeast-1 \
    /tmp/guardrail-response-$i.json
  cat /tmp/guardrail-response-$i.json
  echo ""
  sleep 2
done
```

### 예상 결과

```
=== Attempt 1 ===
{"decision": "ALLOWED", "reason": "All checks passed", "action_count": 1}

=== Attempt 2 ===
{"decision": "ALLOWED", "reason": "All checks passed", "action_count": 2}

=== Attempt 3 ===
{"decision": "ALLOWED", "reason": "All checks passed", "action_count": 3}

=== Attempt 4 ===
{"decision": "BLOCKED", "reason": "Rate limit exceeded: max 3 per hour for volume_resize", "action_count": 3}
```

### CloudWatch 메트릭 확인

```bash
# 가드레일 판정 메트릭
aws cloudwatch get-metric-statistics \
  --namespace FSxNOps/Guardrails \
  --metric-name GuardrailDecision \
  --dimensions Name=Action,Value=volume_resize Name=Decision,Value=BLOCKED \
  --start-time $(date -u -v-1H +%Y-%m-%dT%H:%M:%S) \
  --end-time $(date -u +%Y-%m-%dT%H:%M:%S) \
  --period 300 \
  --statistics Sum \
  --region ap-northeast-1
```

### DynamoDB 상태 확인

```bash
# 가드레일 상태 테이블의 레코드 확인
aws dynamodb get-item \
  --table-name ${PREFIX}-guardrail-state \
  --key '{"action_name": {"S": "volume_resize"}}' \
  --region ap-northeast-1
```

### 예상 DynamoDB 레코드

```json
{
  "action_name": {"S": "volume_resize"},
  "action_count": {"N": "3"},
  "window_start": {"S": "2026-05-23T10:00:00Z"},
  "last_execution": {"S": "2026-05-23T10:05:00Z"},
  "ttl": {"N": "1716544800"}
}
```

### 스크린샷 캡처 포인트

- [ ] Lambda 실행 로그 (ALLOWED → BLOCKED 전환)
- [ ] DynamoDB 테이블 레코드 (TTL 포함)
- [ ] CloudWatch 메트릭의 BLOCKED 카운트

---

## 5. Voice Chat WebRTC 검증

### 전제 조건

- `enableVoiceChat=true`로 배포 완료
- Pipecat Voice Agent가 AgentCore Runtime에 배포 완료 (CLI)
- KVS Signaling Channel 생성 완료

### 테스트 절차

1. CloudFront URL에 접속하여 로그인
2. 채팅 화면에서 마이크 아이콘 클릭
3. 음성 입력 → 텍스트 변환 → RAG 검색 → 음성 출력 플로우 확인

### 확인 포인트

| 단계 | 확인 내용 |
|---------|---------|
| WebRTC 연결 | KVS Signaling Channel을 통해 연결 확립 |
| 음성 입력 | 마이크에서 음성이 캡처됨 |
| STT | 음성이 텍스트로 변환됨 |
| RAG 검색 | 변환된 텍스트로 KB 검색 실행 |
| TTS | 검색 결과가 음성으로 반환됨 |
| 폴백 | WebRTC 실패 시 REST API로 자동 전환 |

### 스크린샷 캡처 포인트

- [ ] Voice Chat UI (마이크 입력 중)
- [ ] KVS Signaling Channel 콘솔 화면
- [ ] WebRTC → REST 폴백 시 로그

---

## 6. 검증 결과 템플릿

각 검증 완료 후 다음 템플릿으로 결과를 기록하세요:

```markdown
## 검증 결과: [유스케이스 이름]

**검증일**: YYYY-MM-DD
**검증자**: [이름]
**환경**: [스택 프리픽스] / [리전]

### 결과 요약

| 테스트 항목 | 결과 | 비고 |
|-----------|------|------|
| [항목 1] | ✅/❌ | [상세] |

### 수집 로그 (마스킹됨)

[CloudWatch Logs / DynamoDB 레코드 / 터미널 출력]

### 스크린샷

[마스킹된 스크린샷 링크]

### 발견 사항·개선점

- [발견 사항 1]
- [발견 사항 2]
```

---

## 7. 마스킹 규칙

공개 전에 다음 정보를 마스킹하세요:

| 대상 | 마스킹 방법 |
|------|-----------|
| AWS 계정 ID | `XXXXXXXXXXXX`로 치환 |
| 리소스 ARN의 계정 부분 | `arn:aws:...:XXXXXXXXXXXX:...` |
| IP 주소 | `xxx.xxx.xxx.xxx`로 치환 |
| SSH 키 지문 | 삭제 |
| Cognito User Pool ID | `ap-northeast-1_XXXXXXXXX`로 치환 |
| Transfer Family Server ID | `s-XXXXXXXXXXXXXXXXX`로 치환 |
| KB ID / Data Source ID | `XXXXXXXXXX`로 치환 |

스크린샷 마스킹에는 기존 스크립트를 사용:

```bash
python3 docs/screenshots/mask_screenshots.py --input screenshot.png --output masked-screenshot.png
```
